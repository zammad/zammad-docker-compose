# Debug scenarios

The files in this folder are not official scenarios. They are development and
debugging tools, undocumented on purpose: not covered by the CI scenario tests,
not listed in `.env.dist`, and they can change or disappear at any time.

Each file documents its own usage in its header comment.
