version: "3.9"
volumes:
  postgresql-data:
    driver: local
    driver_opts: { type: none, o: bind, device: /data/nas/homeserver01/zammad/postgres }
  zammad-storage:
    driver: local
    driver_opts: { type: none, o: bind, device: /data/nas/homeserver01/zammad/storage }
